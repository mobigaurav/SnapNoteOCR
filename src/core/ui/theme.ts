import { MD3LightTheme, type MD3Theme } from 'react-native-paper';

export const theme: MD3Theme = {
  ...MD3LightTheme,
  roundness: 16,
  // Keep colors default for now; we can add a signature brand color next.

};

